# ISSU AI Radar

영업 인에이블먼트 SaaS. Node/Express + Postgres(Supabase) · 빌드 없는 바닐라 JS · 한국어.
`npm run mock` 으로 DB 없이 화면을 띄운다(`127.0.0.1:4173`).

## 깨면 되돌릴 수 없는 것

1. **모르면 판정하지 않는다.** 기본값·평균·추정으로 빈칸을 채우지 않는다. 확인 경로로
   넘긴다. 때우면 낙관적으로 틀리고, 영업이 고객 앞에서 못 지킬 약속을 하게 된다.
2. **개인정보는 `leads` 컬럼에 둔다.** `customer_meta` 에 넣지 않는다 — 거기 두면 `deals`
   로 흘러가 어디까지 퍼졌는지 추적할 수 없다. 수집 항목을 늘리면 화면 고지와
   `PRIVACY_NOTICE.version` 을 **같이** 고친다.
3. **고객이 받는 문서는 내부 필드를 애초에 안 부른다.** 읽고 지우는 방식은 언젠가 샌다.
4. **마이그레이션은 수동 적용**이라 코드가 먼저 배포된다. 새 컬럼을 읽는 코드는
   `hasColumn` 으로 가린다.
5. `npm test` 와 `npm run check` 가 통과해야 커밋한다.

## 시작점

- 기획할 때 — **`issu-plan`** 스킬
- 구현할 때 — **`issu-build`** 스킬 (규약·순서·데인 것)
